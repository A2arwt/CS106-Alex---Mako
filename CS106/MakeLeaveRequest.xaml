<Page x:Class="CS106.AdminLeaveRequest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CS106"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminLeaveRequest">

    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*"></ColumnDefinition>
            <ColumnDefinition Width="40*"></ColumnDefinition>
            <ColumnDefinition Width="30*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid x:Name="pagelist">
            <Grid.RowDefinitions >
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Content="Leave Request" FontSize="32" ></Button>
            <Button Grid.Row="1" Content="Salary Details" FontSize="32" Click="SalaryDetails"></Button>
            <Button Grid.Row="2" Content="Personal Details" FontSize="32" Click="PersonalDetails"></Button>
            <Button Grid.Row="3" Content="Company events/New" FontSize="32" Click="CompanyInfo"></Button>
            <Button Grid.Row="4" Content="Resignation" FontSize="32" Click="Resignation"></Button>
            <Button Grid.Row="5" Content="Logout" FontSize="32" Click="Logout"></Button>
            <Button Grid.Row="6" Content="Exit Employee" FontSize="32" Click="ExitEmployee"></Button>
        </Grid>
        <DatePicker x:Name="StartDate" Grid.Column="1" HorizontalAlignment="Center"  VerticalAlignment="Top"/>
        <DatePicker x:Name="EndDate" Grid.Column="1" HorizontalAlignment="Center"  VerticalAlignment="Center"/>

        <Button Content="Submit" Grid.Column="1" HorizontalAlignment="Center"  VerticalAlignment="Bottom" Height="30" Width="94" Click="Submit"/>
        <TextBox x:Name="Type" Grid.Column="1" HorizontalAlignment="Left"  TextWrapping="Wrap"  VerticalAlignment="Top" Width="120"/>


    </Grid>
</Page>
