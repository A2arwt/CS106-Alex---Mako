<Page x:Class="CS106.ExitEmployee"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CS106"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ExitEmployee">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*"></ColumnDefinition>
            <ColumnDefinition Width="40*"></ColumnDefinition>
            <ColumnDefinition Width="30*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid x:Name="pagelist">
            <Grid.RowDefinitions >
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Content="Manage Leave Request" FontSize="32" Click="LeaveRequestManagement"></Button>
            <Button Grid.Row="1" Content="Manage Salary Details" FontSize="32" Click="SalaryDetails"></Button>
            <Button Grid.Row="2" Content="Manage Employee Details" FontSize="32" Click="PersonalDetails"></Button>
            <Button Grid.Row="3" Content="Company events/New" FontSize="32" Click="CompanyInfo"></Button>
            <Button Grid.Row="5" Content="Logout" FontSize="32" Click="Logout"></Button>
            <Button Grid.Row="4" Content="Exit Employee Management" FontSize="32" Click="Exit"></Button>
        </Grid>
    </Grid>
</Page>
