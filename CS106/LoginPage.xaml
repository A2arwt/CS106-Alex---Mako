<Page x:Class="CS106.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CS106"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">

    <Grid >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="1" Margin="40">
                <Grid Width="400">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="1"
                     Source="D:\Users\270040396\Documents\GitHub\CS106-Alex---Mako\Assests\Style Guide\Logo 2.2 - Transparent.png"
                     Height="212"
                     Width="221"
                     Stretch="Uniform"
                     HorizontalAlignment="Center"
                     Margin="0,5,0,10"/>
                </Grid>

                <TextBox x:Name="Username"
                 Background="#bbe2f2"
                 Width="400"
                 Height="35"
                 HorizontalAlignment="Center"/>

                <PasswordBox x:Name="Password"
                 Background="#bbe2f2"
                 Height="35"
                 Width="400"
                 Margin="0 10 0 0"
                 HorizontalAlignment="Center"/>

                <Grid Margin="0,20,0,0" Width="400">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Button Content="Login"
                    Grid.Column="1"
                    Background="#bbe2f2"
                    Width="100"
                    Height="30"
                    Click="Button_Click"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
